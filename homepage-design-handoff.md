# troxeltech.com homepage design handoff (SUPERSEDED)

Originally a design brief from a 2026-08-04 Cowork brainstorming session, separate from the
Claude Code build sessions. Every decision and spec that was in this document has since been
implemented, decided differently, or migrated verbatim into `CLAUDE.md` (see "Homepage
build" and "TODO: Hero integration + scrollytelling"), which is the authoritative source of
truth. Nothing below - there is nothing below - is a live instruction anymore.

Two decisions from the original brainstorm were explicitly overridden during implementation,
not just migrated - noting that here since "superseded" could otherwise read as "still
roughly right, just moved":

- The palette direction (force a teal-on-near-black identity site-wide) was overridden: the
  site respects system `prefers-color-scheme` instead, per user decision on 2026-08-04.
- The Contact section (a dedicated page section) was overridden: contact info lives in the
  footer instead, per user decision on 2026-08-04.

Nothing identified as still needing this file as its only record. Safe to delete.
